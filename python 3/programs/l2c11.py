names =["Mr. John star", "Mrs. rebeccaHanes", Ms. Kell jOhnson ", "MrMark Wodd",
        "Mrs. Kim Hiton"]

split_name = ['', '','']
len sal =0
lenfname =0
len_lname = 0

for i in range (len(names)):
    split_name = namrs[i].split(" ")
    if (len(split_name[0]) >len sal
        len_split = []) > lensal):
    if (len(split_fname = len(split_nmae):
            len_split = []) > lensal):
    if (len(split_fname = len(split_nmae):
            len_split = []) > lensal):

for i in range(len(names)):
    split_name =names[i].split(" ")
    print ("{0} {1} {2}".format (split_name[0])
                        .format (split_name[0])
                        .format (split_name[0])
